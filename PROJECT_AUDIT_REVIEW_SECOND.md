# 审计修复第二次复核

日期：2026-09-08。项目：`astrohqy20250414`。

对照 `PROJECT_AUDIT_REPORT.md`（A01–A14）与 `PROJECT_AUDIT_REVIEW.md`（R01–R11），复核当前工作区（HEAD 为 `b626961`，包含暂存、未暂存和新增 SQL 文件）。

## 结论

**这次已有实质修复，但仍不能认定之前的问题全部解决。** 点赞重复触发器、编辑历史的事务回滚、普通旧库补列与资料回填、调试按钮绑定已得到修复。字段写入权限、隐私读取、楼层重用、缺配置构建仍有缺口；新迁移和分页存在可复现的边界错误。

本轮列出 9 项主要待修问题（1 项 P1、8 项 P2）。此外，生产调试入口及线上端到端验收仍未完成，见逐项对照表。

## 检查方法与边界

- 未修改业务代码、SQL 或项目依赖；仅新增本报告。未发送邮件、连接生产 Supabase、执行生产迁移或部署。
- 代码发现优先使用知识图谱；图谱覆盖不足及行号过期时，直接核对当前源码、SQL 和配置。
- 经工具授权在临时目录安装 PGlite 0.5.8，运行隔离 PostgreSQL 测试。使用仓库 SQL，去掉测试引擎不需要的 `uuid-ossp`、`pgcrypto` 扩展安装语句；保留表、策略、函数和触发器逻辑。
- 测试数据库模拟 `auth.users`、`auth.uid()` 和 `anon`/`authenticated` 角色，并赋予应用常用的表级 SELECT/INSERT/UPDATE/DELETE 权限。权限测试以这些普通角色执行，不以表所有者绕过 RLS。实际生产如额外配置过列级授权，结果需按实际授权复核。
- 前端边界测试从当前组件提取函数，使用模拟 Supabase 响应执行，未声称完成真实浏览器或邮件端到端测试。
- PGlite 单连接测试可验证约束、权限、触发器和顺序编辑；不能替代真实 PostgreSQL 多连接并发压测、PostgREST 关系缓存及托管验收。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| `npm run build` | 通过，20 个页面 |
| `npm run check` | 通过，38 个文件，0 errors / 0 warnings / 0 hints |
| RSS 链接 | 6/6 对应实际静态文件 |
| 两个认证重写目标 | callback 与 reset-password 的 `index.html` 都存在 |
| 缺少 Supabase 配置 | 构建仍通过，客户端产物仍包含 `https://placeholder.supabase.co` |
| 调试页面 | `/debug-env`、`/auth/debug`、`/test-supabase` 仍生成到生产产物 |
| `npm audit --json` | 4 个受影响包条目：2 high、2 low，与上轮相同 |
| 锁文件/安装版本 | 一致：Astro 5.18.2、Vite 6.4.3、Supabase JS 2.116.0、fast-xml-parser 5.11.1 |
| 全新初始化 | 隔离数据库执行成功 |
| 旧基础库升级 | 补列、回填 profile、外键和楼层成功 |
| 旧基础库 + 旧评论升级脚本 → 新迁移 | 成功；点赞计数 0 → 1 → 0，点赞表仅保留一个业务触发器 |
| 编辑失败回滚 | 正文 CHECK 失败返回 23514；正文与历史均未新增修改 |
| 非法 INSERT 字段 | 成功插入 `likes_count=999` 和伪造创建时间，存在缺口 |
| 匿名读取隐私字段 | 成功读取测试 IP 和编辑前正文 |
| 人工伪造编辑历史 | 评论作者直接插入 `old_content='fake history'` 成功 |
| 删除最高楼层再新增 | 删除 2 楼后新评论仍为 2 楼 |
| 旧用户回填重名 | 返回 23505，`profiles_username_key` 冲突 |
| 重跑迁移校准计数 | 表内 1000、真实点赞 1，校准后仍为 1000 |
| 删除前页评论再翻页 | 40 条加载前 20 条，删除第 1 条，再加载下一页，漏掉第 21 条 |
| 切号期间旧点赞请求完成 | B 的点赞集合被 A 的旧请求回调写入 `c1` |

`git diff --check` 另发现若干行尾空格，未将其计为功能缺陷。

## 待修问题

### S01 — P1：资料回填的新用户名算法可使合法旧库迁移失败（R01 / A02）

位置：`supabase-migration-upgrade.sql:71–78`；同类逻辑见 `supabase-setup-complete.sql:64–71`。

旧基础脚本对没有 username 元数据的用户使用 `user_` 加 UUID 前缀。新迁移不复制原资料表，而是从邮箱 `@` 前的部分重新生成 username；两位用户分别为 `same@example.test` 与 `same@other.test` 时，旧库中的两个用户名合法且不同，新库回填却都生成 `same`。`ON CONFLICT (id)` 不能处理另一个唯一键 `username` 的冲突。

**隔离数据库已复现：** 旧脚本建库并成功插入两个不同 UUID 前缀的账号后，执行新迁移返回 `23505 duplicate key value violates unique constraint "profiles_username_key"`，升级中止。

建议优先迁移原资料表的有效资料，缺失时使用稳定的唯一后缀，迁移前检查所有用户名碰撞；不要简单对所有冲突 DO NOTHING，否则会漏建 profile 并再次破坏评论外键。新用户触发器中的相同邮箱前缀回退逻辑也应统一处理。冲突目标的含义可参见 [PostgreSQL INSERT 文档](https://www.postgresql.org/docs/current/sql-insert.html)。

### S02 — P2：保护触发器只覆盖 UPDATE，INSERT 和历史表仍允许伪造（R03 / A07）

位置：`supabase-setup-complete.sql:213–217`、`:272–277`、`:314–325`；迁移脚本对应 `:263–267`、`:316–318`、`:344–352`。

现在更新已有评论时，计数、文章归属、楼层等字段确实会被保护。但客户端创建评论仍只需满足 `auth.uid() = user_id`，没有列级写入限制，也没有插入时初始化全部系统字段的触发器。楼层会被强制覆盖，其余字段不会。

**已复现：** 普通作者成功插入 `likes_count=999`、自定义 `ip_address`、`created_at='2000-01-01'`；随后另一个账号真实点赞使计数变为 1000。作者还可以直接向历史表插入任意 `old_content`，并未发生对应正文编辑。

建议限制 INSERT/UPDATE 可写列或使用受约束 RPC，由数据库生成计数和时间；撤销客户端直接写编辑历史的权限，由触发器独占该写入路径。还需覆盖 `is_edited`、`original_content`、`updated_at` 等未被完整保护的字段。

### S03 — P2：前端字段白名单没有关闭数据库公开读取（R04 / A13）

位置：`supabase-setup-complete.sql:267–270`、`:309–312`；`supabase-migration-upgrade.sql:312–314`、`:340–342`。

评论列表现在只请求显示字段，避免正常页面顺带下载 IP，这是有效改进。但 comments 与 comment_edit_history 的 SELECT 策略仍然 `USING (true)`，没有撤销敏感列的公开读取权限。

**已复现：** `SET ROLE anon` 后可以直接 SELECT 评论 IP，也可以读取触发器自动生成的旧正文。旧正文归档修好以后，用户编辑删除的内容反而会更可靠地留在公开可查询的历史表里。

建议用受限历史表、列级授权或公开视图隔离数据，并撤销原表过宽的 SELECT 授权。RLS 决定可访问的行，字段权限需另外设置，参见 [PostgreSQL 行安全和列权限示例](https://www.postgresql.org/docs/current/ddl-rowsecurity.html)。

### S04 — P2：楼层仍然重用，也没有处理旧重复楼层（R06 / A08）

位置：`supabase-setup-complete.sql:141–149`、`:119–123`；`supabase-migration-upgrade.sql:140–165`、`:196–202`。

`MAX(floor_number)+1` 没有保存已删除的最大编号。已复现删除 2 楼后，新评论重新取得 2 楼。迁移现在从最大值之后回填 NULL，这是修复；但已有的非 NULL 重号不会被处理，所谓“楼层唯一约束”仍只是普通 `CREATE INDEX`。

建议按文章保存持久计数器，迁移时清理已有重复，建立 `(post_slug, floor_number)` 唯一约束，并使用确定性的排序。多连接并发分配仍需单独验收。

### S05 — P2：配置缺失仍会生成不可用认证客户端（R07 / A12）

位置：`src/lib/supabaseClient.ts:28–35`。

本次实际构建继续成功，产物仍带占位 Supabase 地址。`isSupabaseConfigured` 仍只控制日志和占位配置，没有生产构建失败机制，也没有覆盖登录、评论、找回密码等所有入口的统一访客模式。

建议在构建配置层校验必要变量并失败退出，或者明确实现访客模式并阻止相关请求。只增加 console.warn 不能关闭此项。

### S06 — P2：按固定偏移翻页，删除已加载评论后会漏数据（R09 / A14 的分页回归）

位置：`src/components/CommentList.jsx:46–48`、`:80`、`:114–121`。

页码乘以 20 决定下一次 offset；删除仅从本地数组移除评论，下一页起点不变。前面少一条记录后，数据库其余行的位置前移，固定 offset 会跳过尚未加载的一条。

**提取当前函数复现：** 共 40 条，加载 1–20，删除 1，再加载下一页；结果只显示 38 条，而数据库有 39 条，漏掉 21。其他用户删除前页记录也会发生相同问题。

建议使用稳定排序键的游标分页；若暂时保持 offset，删除后应重新加载并重建分页状态。另一个残留规模问题是 `:155` 将全部累计加载的评论 ID 放入 `.in()`，并非注释所说的“仅查当前页”；应按新增页查询并合并结果，控制单次 URL 长度。

### S07 — P2：旧账号点赞写请求的回调仍能污染新账号状态（R08 / A10）

位置：`src/components/CommentItem.jsx:84–88`、`:67–71`；`src/components/CommentList.jsx:124–137`。

新代码会在切号时清空集合，查询失败时也清空，父子状态现在同步；这些修复有效。但只有查询 effect 有取消标记，点赞/取消点赞的写请求没有校验响应对应的账号。A 点击点赞后切到 B，B 的查询完成后，A 的旧响应再调用 `onLikeToggled`，会直接改写当前父集合。回调更新 `fetchedComments` 后还会触发新的查询，但该查询完成之前存在错误状态和操作窗口；查询失败也没有阻止这次旧回调。

**提取当前 handleLike 函数复现：** 将 A 的插入响应延迟到 B 的空点赞查询结果之后，再返回成功，B 集合被写入 A 点赞的 `c1`。

建议在操作开始时绑定 user.id 与请求代次，响应后校验当前身份；父回调也应接收和校验身份。切号及点赞状态尚未加载时，应避免使用上一代状态决定操作分支。

### S08 — P2：事务编辑已修复，但并发编辑仍静默覆盖（R05 / A09 的剩余部分）

位置：`src/components/CommentItem.jsx:136–143`。

当前前端只发一次正文 UPDATE，数据库用 `OLD.content` 自动归档；约束失败时历史和正文一起回滚，确认不应继续报告“两次请求不原子”。但更新条件仍只有评论 ID，没有版本或原更新时间条件。

**隔离数据库复现：** 两个编辑者视图读取相同旧正文，A 保存后，B 用旧视图提交依然成功，最终正文被 B 覆盖。每次更新有可信自动历史，不能代替用户可见的编辑冲突处理。

建议增加版本号条件并检查实际返回/影响行数；版本不匹配时提示重新加载。不要只判断 Supabase `error` 是否为空。

### S09 — P2：已安装字段保护后，迁移的计数校准会被静默抵消（R01/R02 迁移回归）

位置：`supabase-migration-upgrade.sql:167–173`、`:256–258`。

迁移开头直接 UPDATE likes_count，后面才重建保护触发器。对已执行过新完整脚本或本迁移的库，保护触发器已经存在；校准 UPDATE 没有内部计数标记，触发器把新计数改回 OLD.likes_count。表所有者权限也不会自动跳过该触发器。

**已复现：** 评论计数为 1000，点赞表实际只有 1 行；执行完整迁移成功返回，但计数仍为 1000。这使脚本宣称的“校准存量点赞数”在重复执行或新完整脚本后升级时失效。

建议在明确事务中有序处理旧保护触发器与数据校准，使用仅迁移所有者可达的内部路径，并在结束前验证存储计数等于真实点赞聚合。不要通过开放普通用户绕过字段保护来解决。

## 上轮 R01–R11 逐项对照

| 上轮编号 | 本轮判断 |
| --- | --- |
| R01 旧库升级 | 普通基础库升级已通过；补列、回填资料、直接外键有效。仍有 S01 用户名碰撞和 S09 重跑校准缺陷。文档已正确区分新库与旧库，不再要求旧库直接跑完整初始化 |
| R02 重复点赞触发器 | 已修复：旧升级库执行迁移后仅一个点赞业务触发器，他人点赞/取消为 0→1→0。计数历史修复仍受 S09 影响 |
| R03 字段保护 | UPDATE 的主要字段已保护；INSERT 和历史直接写入未收紧，见 S02 |
| R04 隐私读取 | 前端白名单有效；数据库直接读取仍公开，见 S03 |
| R05 编辑事务 | 原子更新、可信 OLD 正文和失败回滚已修复；并发冲突控制剩余 S08，历史伪造入口见 S02 |
| R06 楼层 | NULL 回填从最大值继续已修复；永久不重用、旧重号清理和唯一约束仍未完成，见 S04 |
| R07 配置 | 未修复，实际构建确认，见 S05 |
| R08 点赞状态 | 换号清空、失败清空、父子同步有效；旧账号写请求晚返回仍有 S07 |
| R09 分页 | 已有每页 20 条和加载更多入口；新增删除后漏记录问题及累计 ID 查询，见 S06 |
| R10 调试按钮 | 已恢复 `testPasswordReset()` 并绑定到 window，按钮调用发送邮件，原 ReferenceError 与按钮语义不符问题已修复；未实际发送邮件 |
| R11 日志与调试入口 | test-supabase 不再打印完整注册响应，debug-env 改为会话摘要；但三个调试页面仍公开生成，auth/debug 仍导出把密码设成 `testpassword123` 的测试函数，且原样显示 URL（含可能存在的恢复令牌）。生产入口隔离尚未完成 |

## 原 A01–A14 的补充结论

- A01：占位重置页和独立 CDN 已移除；真实恢复邮件、链接过期、PKCE/implicit 自动恢复与刷新行为仍待真实 Supabase 验收。
- A02、A06–A14：以上 R 表和 S 问题已覆盖；不能把局部通过当作整项关闭。
- A03：默认本机监听和 Vite 6.4.3 的既有修复保留。依赖审计仍为 4 个受影响包条目，并非 4 个已证实的线上攻击入口。没有对全部剩余公告完成可达性验收。例如 [Astro Host header SSRF 公告](https://github.com/withastro/astro/security/advisories/GHSA-2pvr-wf23-7pc7) 要求特定 SSR 错误页请求路径，当前静态产物不足以构成该条件；[sharp/libvips 公告](https://github.com/lovell/sharp/security/advisories/GHSA-f88m-g3jw-g9cj) 仍需结合构建时图片来源继续评估。
- A04：本轮 RSS 6/6 链接仍通过。
- A05：两个认证页重写目标均与静态文件一致，reset-password 缺少对应配置的上轮问题已修；Vercel 预览部署路由、尾斜杠和 hash 保留尚未验收。

建议先修 S01–S03、S09 的迁移与数据边界，再修 S04–S08；完成后在测试 Supabase 做真实账号、邮件和数据库端到端验收。
